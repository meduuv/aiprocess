[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="aiprocess"
version="0.1.0"
description="Lightweight AI text processing helpers"
requires-python=">=3.10"
[tool.setuptools.packages.find]
where=["src"]
